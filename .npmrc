# Keep transitive deps out of the root resolution surface — each workspace must
# declare what it imports. Prevents apps/api accidentally compiling against a
# package only apps/web depends on.
strict-peer-dependencies=false
auto-install-peers=true
