# ==========================================================================
# CRE SECRETS FILE
# ==========================================================================
# Maps the secret ID workflow.ts requests with runtime.getSecret({ id }) to
# the environment variable holding its value. In a Confidential Workflow
# these are released by the Vault DON directly into the attested enclave.
secretsNames:
    VENDOR_LOOKUP_API_KEY:
        - SECRET_VENDOR_LOOKUP_API_KEY
