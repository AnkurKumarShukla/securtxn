{
  "name": "@cp/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "deploy:testnet": "tsx scripts/deploy-testnet.ts"
  },
  "dependencies": {
    "@cp/shared-types": "workspace:*",
    "@hashgraph/asset-tokenization-sdk": "^1.0.0",
    "@hashgraph/sdk": "^2.53.0"
  },
  "devDependencies": { "tsx": "^4.19.0", "typescript": "^5.6.0", "vitest": "^2.1.0" }
}
